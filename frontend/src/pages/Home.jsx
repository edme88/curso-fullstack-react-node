import { useTasks } from "../hooks/useTasks";

const Home = () => {
    //const { tasks, loader, error } = useTasks;

    return (
        <>
            <h2>Hola Home</h2>
            <h2>Chau</h2>
        </>
    );
}

export {Home};